using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using QuestSystem;

public class QuestTest : MonoBehaviour
{
    //public Quest_Json quest;
    // Start is called before the first frame update
    void Start()
    {
        //quest = new Quest_Json();
        //QuestDatabase.Quest_SaveAsJson(quest);
        //quest = QuestDatabase.Quest_LoadAsJson();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
